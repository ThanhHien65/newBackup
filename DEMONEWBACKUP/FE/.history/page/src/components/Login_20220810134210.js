import React, { useState } from "react";
const Login = () => {
  const redirect = (e) => {
    e.preventDefault();
  };
  return (
    <div className="Auth-form-container">

    </div>
  );
};

export default Login;
